# Development and build dependencies (not needed at runtime).
# Install with: pip install -r requirements-dev.txt

# Packaging
pyinstaller>=6.0

# Testing
pytest>=8.0

# Linting / formatting
ruff>=0.4
