# Desktop app build dependencies.
# Install these in addition to requirements.txt to build the standalone
# FunscriptForge desktop app (Windows, macOS, Linux).
#
#   pip install -r requirements.txt -r requirements-desktop.txt
#   pyinstaller FunscriptForge.spec --clean

pywebview>=5.0
pyinstaller>=6.0
psutil>=5.9  # clean subprocess teardown on Windows
